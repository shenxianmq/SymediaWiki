# Symedia 二级分类使用教程

## 📖 目录

- [基础概念](#基础概念)
- [配置文件结构](#配置文件结构)
- [匹配规则详解](#匹配规则详解)
- [Mode 模式详解](#mode-模式详解)
- [Series 系列分类](#series-系列分类)
- [高级用法](#高级用法)
- [实战示例](#实战示例)
- [常见问题](#常见问题)
- [参考字典](#参考字典)

---

## 基础概念

### 什么是二级分类？

二级分类是在媒体库中按照特定规则对影片进行自动分类的功能。例如：

```
电影/
├── 漫威系列/
│   ├── 钢铁侠系列/
│   │   ├── 钢铁侠 (2008)/
│   │   └── 钢铁侠2 (2010)/
│   └── 复仇者联盟系列/
├── 华语电影/
│   └── 让子弹飞 (2010)/
└── 欧美电影/
    └── 盗梦空间 (2010)/
```

### 配置文件位置

配置文件路径：`config/category.yaml`

---

## 配置文件结构

### 基本格式

```yaml
# 电影分类配置
movie:
  分类名称1:
    匹配条件1: "值"
    匹配条件2: "值"
  分类名称2:
    匹配条件: "值"

# 电视剧分类配置
tv:
  分类名称1:
    匹配条件: "值"
```

### 重要说明

1. **分类名称** = **目录名称**
2. **按顺序匹配**，匹配成功后停止
3. **空配置** = **保底分类**（匹配所有未分类的）
4. **多个条件** 默认为 AND 关系（都要满足）

---

## 匹配规则详解

### 常用匹配字段

| 字段                   | 说明                 | 示例                         |
| ---------------------- | -------------------- | ---------------------------- |
| `genre_ids`            | 内容类型             | `"28"` (动作), `"16"` (动画) |
| `original_language`    | 原始语言             | `"zh,en,ja"`                 |
| `production_countries` | 制作国家（电影）     | `"US,CN,JP"`                 |
| `origin_country`       | 制作国家（电视剧）   | `"US,CN,JP"`                 |
| `keywords`             | 关键词（从标题匹配） | `"漫威,复仇者"`              |
| `year`                 | 年份                 | `"2020-2025"`                |
| `series_keywords`      | 系列关键词           | `"钢铁侠,蜘蛛侠"`            |
| `series_directors`     | 系列导演             | `"诺兰,斯皮尔伯格"`          |
| `series_actors`        | 系列演员             | `"汤姆·克鲁斯"`              |
| `mode`                 | 匹配模式             | `"and"` 或 `"or"`            |

### 多值匹配

使用**逗号**分隔多个值：

```yaml
华语电影:
  original_language: "zh,cn,bo,za" # 匹配中文、粤语、藏语、壮语
```

### 操作符

#### `+` 号 - 必须包含

```yaml
儿童动画:
  genre_ids: "+16,+10751" # 必须同时是动画(16)和家庭(10751)
```

#### `-` 号 - 必须排除

```yaml
纪录片:
  genre_ids: "99,-10402" # 是纪录片(99)但不是音乐(10402)
```

#### 混合使用

```yaml
动作冒险:
  genre_ids: "+28,12,14,-27" # 必须有动作(28)，可以有冒险(12)或奇幻(14)，但不能有恐怖(27)
```

### 年份范围

```yaml
近期电影:
  year: "2020-2025" # 2020年到2025年之间
```

---

## Mode 模式详解

### 什么是 Mode？

`mode` 字段控制多个条件之间的逻辑关系：

- **`mode: "and"`** (默认) - 所有条件都要满足
- **`mode: "or"`** - 任意一个条件满足即可

### AND 模式（默认）

不写 `mode` 或 `mode: "and"` 时，所有条件都要满足：

```yaml
美国科幻片:
  # mode: "and"  # 可以不写，默认就是 and
  genre_ids: "878" # 条件1：必须是科幻
  production_countries: "US" # 条件2：必须是美国
  year: "2020-2025" # 条件3：必须是2020-2025年
```

**匹配结果**：只有**同时满足**以上三个条件的才会被分类到这里。

### OR 模式

设置 `mode: "or"` 时，任意条件满足即可：

```yaml
热门影片:
  mode: "or"
  genre_ids: "28,12,878" # 条件1：动作、冒险或科幻
  keywords: "漫威,DC" # 条件2：包含这些关键词
  year: "2024-2025" # 条件3：这两年的新片
```

**匹配结果**：满足**任意一个**条件就会被分类到这里。

### 使用场景对比

#### 场景 1：精确分类（用 AND）

```yaml
经典港片:
  mode: "and" # 所有条件都要满足
  origin_country: "HK"
  year: "1980-2000"
  genre_ids: "28,80" # 动作或犯罪
```

只有同时满足：香港 + 1980-2000 年 + (动作或犯罪) 的影片才匹配。

#### 场景 2：广泛收集（用 OR）

```yaml
动作大片:
  mode: "or" # 任意条件满足即可
  genre_ids: "28" # 动作片
  keywords: "爆炸,枪战,追车" # 或包含这些关键词
  series_actors: "杰森·斯坦森,范·迪塞尔" # 或这些演员主演
```

只要满足任意一个条件就匹配。

---

## Series 系列分类

### 什么是 Series？

Series 属性用于生成**子分类**，格式为：`分类名/系列名`

例如：

- `漫威系列/钢铁侠系列/`
- `漫威系列/复仇者联盟系列/`

### Series 属性类型

| 属性               | 说明                 | 示例              |
| ------------------ | -------------------- | ----------------- |
| `series_keywords`  | 从标题匹配系列关键词 | `"钢铁侠,蜘蛛侠"` |
| `series_directors` | 从导演匹配系列       | `"诺兰,昆汀"`     |
| `series_actors`    | 从演员匹配系列       | `"汤姆·克鲁斯"`   |

### 重要特性

⚠️ **Series 属性不参与 OR 模式的条件判断，只用于生成系列名！**

### 使用方式

#### 方式 1：只有 Series 属性

```yaml
漫威系列:
  series_keywords: "钢铁侠,美国队长,雷神,复仇者联盟"
```

**效果**：

- 标题包含"钢铁侠" → `漫威系列/钢铁侠系列/`
- 标题包含"复仇者联盟" → `漫威系列/复仇者联盟系列/`
- 标题不包含任何关键词 → 不匹配

#### 方式 2：AND 模式 + Series

```yaml
科幻系列:
  mode: "and" # 默认值，可不写
  genre_ids: "878" # 必须是科幻片
  production_countries: "US" # 必须是美国
  series_keywords: "星球大战,星际迷航" # 可选：生成系列名
```

**效果**：

- 美国科幻片 + 标题含"星球大战" → `科幻系列/星球大战系列/`
- 美国科幻片 + 标题不含关键词 → `科幻系列/`
- 中国科幻片 → 不匹配（条件不满足）

#### 方式 3：OR 模式 + Series

```yaml
动作系列:
  mode: "or"
  genre_ids: "28" # 条件1：动作片
  keywords: "汽车,赛车" # 条件2：关键词
  series_keywords: "速度与激情,变形金刚" # 只生成系列名，不参与条件判断
```

**效果**：

- 动作片 + 标题含"速度与激情" → `动作系列/速度与激情系列/`
- 动作片 + 标题不含系列关键词 → `动作系列/`
- 剧情片 + 标题含"汽车" → `动作系列/` (keywords 匹配)
- 剧情片 + 标题含"速度与激情" → 不匹配（普通条件不满足）

⚠️ **注意**：在 OR 模式下，series_keywords 匹配**不会**让 `match_flag` 变为 true！

---

## 高级用法

### 1. 排除特定类型

```yaml
剧情片:
  genre_ids: "18,-27,-16" # 剧情片，但不要恐怖片和动画
```

### 2. 多条件组合

```yaml
亚洲动作片:
  mode: "and"
  genre_ids: "28"
  origin_country: "CN,HK,TW,JP,KR,TH"
  year: "2020-2025"
```

### 3. 保底分类

放在最后，不配置任何条件：

```yaml
其他电影:
  # 不配置条件，匹配所有前面未匹配的
```

或配置为空：

```yaml
其他电影:
```

### 4. 导演/演员系列

```yaml
诺兰作品:
  series_directors: "克里斯托弗·诺兰"

汤姆·克鲁斯电影:
  series_actors: "汤姆·克鲁斯"
```

### 5. 复杂的 OR 逻辑

```yaml
假期观影推荐:
  mode: "or"
  genre_ids: "35,10751" # 喜剧或家庭
  keywords: "圣诞,新年,假期"
  year: "2023-2025" # 或最近新片
```

---

## 实战示例

### 示例 1：电影完整配置

```yaml
movie:
  # 1. 特定系列（优先匹配）
  漫威系列:
    series_keywords: "钢铁侠,美国队长,雷神,复仇者联盟,银河护卫队,蜘蛛侠"

  DC系列:
    series_keywords: "蝙蝠侠,超人,神奇女侠,正义联盟"

  # 2. 特定类型
  纪录片:
    genre_ids: "99,-10402" # 纪录片但不包括音乐类

  演唱会:
    mode: "or"
    genre_ids: "10402"
    keywords: "演唱会,巡演,live"

  # 3. 动画电影
  国产动画:
    mode: "and"
    genre_ids: "16"
    production_countries: "CN"

  日本动画:
    mode: "and"
    genre_ids: "16"
    production_countries: "JP"
    series_keywords: "哆啦A梦,柯南,火影忍者"

  # 4. 地区分类
  华语电影:
    original_language: "zh,cn"

  日韩电影:
    original_language: "ja,ko"

  # 5. 保底分类
  欧美电影:
```

### 示例 2：电视剧完整配置

```yaml
tv:
  # 1. 动漫分类（优先）
  国漫:
    mode: "and"
    genre_ids: "16"
    origin_country: "CN,TW,HK"

  日漫:
    mode: "and"
    genre_ids: "16"
    origin_country: "JP"

  欧美动漫:
    mode: "and"
    genre_ids: "16"
    origin_country: "US,GB,FR"

  # 2. 特定类型
  纪录片:
    genre_ids: "99"

  综艺:
    genre_ids: "10764,10767"

  儿童节目:
    genre_ids: "10762"

  # 3. 地区剧集
  国产剧:
    origin_country: "CN,TW,HK,SG"
    series_keywords: "长相思,庆余年,三体"

  日剧:
    origin_country: "JP"

  韩剧:
    origin_country: "KR"
    series_keywords: "爱的迫降,鱿鱼游戏"

  泰剧:
    origin_country: "TH"

  # 4. 欧美剧
  欧美剧:
    origin_country: "US,GB,CA,DE,FR"

  # 5. 保底
  其他剧集:
```

### 示例 3：Mode 混合使用

```yaml
movie:
  # 精确匹配：经典港片
  经典港片:
    mode: "and"
    origin_country: "HK"
    year: "1980-2000"
    genre_ids: "28,80,18" # 动作/犯罪/剧情

  # 广泛匹配：热门影片
  热门大片:
    mode: "or"
    genre_ids: "28,12,878,14" # 动作/冒险/科幻/奇幻
    keywords: "票房,口碑"
    year: "2023-2025"

  # 系列收集：导演作品
  大师作品:
    mode: "or"
    series_directors: "克里斯托弗·诺兰,昆汀·塔伦蒂诺,斯皮尔伯格,詹姆斯·卡梅隆"
```

---

## 常见问题

### Q1: 分类顺序重要吗？

**非常重要！** 程序会按配置顺序匹配，匹配成功后停止。

❌ **错误示例**（宽泛的在前）：

```yaml
movie:
  欧美电影: # 这个太宽泛，几乎匹配所有
  漫威系列: # 永远不会匹配到这里
    series_keywords: "复仇者联盟"
```

✅ **正确示例**（具体的在前）：

```yaml
movie:
  漫威系列: # 先匹配具体的
    series_keywords: "复仇者联盟"
  欧美电影: # 再匹配宽泛的
```

### Q2: 怎么调试分类不生效？

1. 检查配置语法是否正确（YAML 格式）
2. 检查分类顺序（是否被前面的分类拦截）
3. 查看日志输出
4. 确认 TMDB 数据是否包含该字段

### Q3: Mode 什么时候用 AND，什么时候用 OR？

- **AND（默认）**：需要精确分类时使用
  - 例如："2020 年后的美国科幻片"
- **OR**：需要广泛收集时使用
  - 例如："漫威电影 或 DC 电影 或 2024 年新片"

### Q4: Series 属性在 OR 模式下怎么工作？

Series 属性**不参与** OR 条件判断，只用于生成系列名。

```yaml
动作系列:
  mode: "or"
  genre_ids: "28" # 条件1
  keywords: "爆炸" # 条件2
  series_keywords: "速度与激情" # 不是条件，只生成系列名
```

- 动作片 → 匹配 ✓
- 标题含"爆炸" → 匹配 ✓
- 仅标题含"速度与激情" → **不匹配** ✗ （因为 series_keywords 不算 OR 条件）

### Q5: 能否让 Series 也参与 OR 判断？

不能。这是设计决策。如果需要关键词参与判断，请使用 `keywords` 而不是 `series_keywords`：

```yaml
动作系列:
  mode: "or"
  genre_ids: "28"
  keywords: "速度与激情" # 参与 OR 判断
```

### Q6: 怎么处理多语言标题？

`keywords` 和 `series_keywords` 从 **TMDB 标题** 匹配，使用本地化标题：

```yaml
日本动画:
  genre_ids: "16"
  origin_country: "JP"
  series_keywords: "ワンピース,海贼王,航海王" # 多个译名
```

### Q7: 年份范围的边界是包含还是排除？

**包含**边界。`"2020-2025"` 包括 2020 和 2025 年。

---

## 参考字典

### Genre IDs（内容类型）

#### 电影类型

| ID    | 中文     | English         |
| ----- | -------- | --------------- |
| 28    | 动作     | Action          |
| 12    | 冒险     | Adventure       |
| 16    | 动画     | Animation       |
| 35    | 喜剧     | Comedy          |
| 80    | 犯罪     | Crime           |
| 99    | 纪录     | Documentary     |
| 18    | 剧情     | Drama           |
| 10751 | 家庭     | Family          |
| 14    | 奇幻     | Fantasy         |
| 36    | 历史     | History         |
| 27    | 恐怖     | Horror          |
| 10402 | 音乐     | Music           |
| 9648  | 悬疑     | Mystery         |
| 10749 | 爱情     | Romance         |
| 878   | 科幻     | Science Fiction |
| 10770 | 电视电影 | TV Movie        |
| 53    | 惊悚     | Thriller        |
| 10752 | 战争     | War             |
| 37    | 西部     | Western         |

#### 电视剧类型

| ID    | 说明     |
| ----- | -------- |
| 10759 | 动作冒险 |
| 16    | 动画     |
| 35    | 喜剧     |
| 80    | 犯罪     |
| 99    | 纪录片   |
| 18    | 剧情     |
| 10751 | 家庭     |
| 10762 | 儿童     |
| 9648  | 悬疑     |
| 10763 | 新闻     |
| 10764 | 真人秀   |
| 10765 | 科幻奇幻 |
| 10766 | 肥皂剧   |
| 10767 | 脱口秀   |
| 10768 | 战争政治 |
| 37    | 西部     |

### Language Codes（语言代码）

| 代码 | 语言     | 代码 | 语言     |
| ---- | -------- | ---- | -------- |
| zh   | 中文     | en   | 英语     |
| ja   | 日语     | ko   | 韩语     |
| fr   | 法语     | de   | 德语     |
| es   | 西班牙语 | it   | 意大利语 |
| pt   | 葡萄牙语 | ru   | 俄语     |
| ar   | 阿拉伯语 | hi   | 印地语   |
| th   | 泰语     | vi   | 越南语   |
| id   | 印尼语   | tr   | 土耳其语 |

### Country Codes（国家代码）

| 代码 | 国家/地区 | 代码 | 国家/地区 |
| ---- | --------- | ---- | --------- |
| CN   | 中国大陆  | HK   | 中国香港  |
| TW   | 中国台湾  | MO   | 中国澳门  |
| US   | 美国      | GB   | 英国      |
| JP   | 日本      | KR   | 韩国      |
| FR   | 法国      | DE   | 德国      |
| IT   | 意大利    | ES   | 西班牙    |
| CA   | 加拿大    | AU   | 澳大利亚  |
| IN   | 印度      | TH   | 泰国      |
| SG   | 新加坡    | MY   | 马来西亚  |
| RU   | 俄罗斯    | BR   | 巴西      |
| MX   | 墨西哥    | AR   | 阿根廷    |

---

## 快速上手

### 第一步：复制模板

复制示例配置到 `config/category.yaml`

### 第二步：测试简单配置

```yaml
movie:
  华语电影:
    original_language: "zh,cn"

  其他电影:
```

### 第三步：逐步完善

根据实际需求，添加更多分类。

### 第四步：观察效果

查看媒体库目录结构是否符合预期。

---

## 总结

- ✅ 分类按顺序匹配，具体的放前面
- ✅ 默认 AND 模式，需要 OR 时显式指定
- ✅ Series 属性只生成系列名，不参与 OR 判断
- ✅ 使用操作符 `+` `-` 精确控制匹配
- ✅ 保底分类放最后，配置为空即可
- ✅ 多测试，查看日志确认效果

---

**祝你使用愉快！如有问题，欢迎查看日志或提 issue。** 🎉
