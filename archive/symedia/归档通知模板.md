# 归档通知模板

## 模板 1：🏮「雅集珍藏」主题 🏮 (古风)

```json
{
    "title": "📜 喜报！「{{ title_year }}{% if season_episode %}之{{ season_episode }}{% endif %}」已入翰林珍藏阁！",
    "text": (
        "{% if vote_average %}🌟 雅士清赏：{{ vote_average }}分 (上品佳作)\n{% endif %}"
        "📚 藏品类别：{{ media_type }} (如诗如画)\n"
        "{% if category %}🏷️ 风雅品鉴：{{ category }}\n{% endif %}"
        "{% if resource_quality %}💎 画质墨韵：{{ resource_quality }} (纤毫毕现)\n{% endif %}"
        "📖 卷册数目：{{ file_count }} 卷\n"
        "⚖️ 府库存储：{{ total_size }}\n"
        "🎬 天机阁引 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👨‍🎨 妙手丹青/译制：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 录入耗时：{{ time_usage }} (弹指之间)\n{% endif %}"
        "🍵 请君慢品。\n"
    )
}
```

## 模板 2：🎮「游戏通关」主题 🎮

```json
{
    "title": "🏆 {{ title_year }} {{ season_episode if season_episode else '' }} - 新成就解锁！🎉",
    "text": (
        "{% if vote_average %}⭐ 玩家评分：{{ vote_average }}/10\n{% endif %}"
        "📚 游戏类型：{{ media_type }}\n"
        "{% if category %}🏷️ 游戏标签：{{ category }}\n{% endif %}"
        "{% if resource_quality %}📈 画质设定：{{ resource_quality }}\n{% endif %}"
        "📦 文件包：{{ file_count }}个\n"
        "💾 硬盘占用：{{ total_size }}\n"
        "🎮 游戏 ID (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👨‍💻 开发团队：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 下载/安装用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 3：🍕「美味派送」主题 🍕

```json
{
    "title": "🛵 热腾腾的「{{ title_year }} {{ season_episode if season_episode else '' }}」已送达！🍕",
    "text": (
        "{% if vote_average %}🌟 食客推荐指数：{{ vote_average }}/10\n{% endif %}"
        "📚 菜品类型：{{ media_type }}\n"
        "{% if category %}🏷️ 口味标签：{{ category }}\n{% endif %}"
        "{% if resource_quality %}🌶️ 新鲜度/画质：{{ resource_quality }}\n{% endif %}"
        "🍽️ 分量：{{ file_count }}份\n"
        "⚖️ 总重量：{{ total_size }}\n"
        "🎬 订单号 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👨‍🍳 出品方：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 派送用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 4：🕵️「绝密档案」主题 🕵️

```json
{
    "title": "🔒 [机密] 「{{ title_year }} {{ season_episode if season_episode else '' }}」档案已入库！🤫",
    "text": (
        "{% if vote_average %}⭐ 情报价值评估：{{ vote_average }}/10\n{% endif %}"
        "📚 档案类型：{{ media_type }}\n"
        "{% if category %}🏷️ 秘密分类：{{ category }}\n{% endif %}"
        "{% if resource_quality %}📊 情报清晰度：{{ resource_quality }}\n{% endif %}"
        "🗂️ 文件夹数量：{{ file_count }}个\n"
        "💾 占用存储：{{ total_size }}\n"
        "🎬 档案编号 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👤 情报来源：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 解密用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 5：🎉「派对时间」主题 🎉

```json
{
    "title": "🥳 {{ title_year }} {{ season_episode if season_episode else '' }} 已加入派对清单！LET'S GOOO! 🎊",
    "text": (
        "{% if vote_average %}🌟 派对热度：{{ vote_average }}/10 🔥\n{% endif %}"
        "🎶 派对类型：{{ media_type }}\n"
        "{% if category %}🏷️ 主题风格：{{ category }}\n{% endif %}"
        "{% if resource_quality %}🔊 音画质量：{{ resource_quality }}\n{% endif %}"
        "🎁 惊喜数量：{{ file_count }}个\n"
        "🎈 场地占用：{{ total_size }}\n"
        "🎫 派对 ID (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}🎧 DJ/主办方：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 筹备用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 6：🐉「神兽召唤」主题 🐉

```json
{
    "title": "✨ {{ title_year }} {{ season_episode if season_episode else '' }} - 稀有神兽已成功捕获！🐲",
    "text": (
        "{% if vote_average %}🌟 神兽战力指数：{{ vote_average }}/10\n{% endif %}"
        "📚 属性：{{ media_type }}\n"
        "{% if category %}🏷️ 栖息地/种类：{{ category }}\n{% endif %}"
        "{% if resource_quality %}🧬 血脉纯度：{{ resource_quality }}\n{% endif %}"
        "🧩 灵魂碎片：{{ file_count }}块\n"
        "⚖️ 体重：{{ total_size }}\n"
        "🐾 神兽图鉴编号 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👨‍🔬 捕获者：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 捕获耗时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 7：🏝️「海盗宝藏」主题 🏝️

```json
{
    "title": "💰 {{ title_year }} {{ season_episode if season_episode else '' }} - 新的宝藏已入库！🏴‍☠️",
    "text": (
        "{% if vote_average %}🌟 宝藏评级：{{ vote_average }}/10 💎\n{% endif %}"
        "📚 宝藏类型：{{ media_type }}\n"
        "{% if category %}🏷️ 藏宝图标记：{{ category }}\n{% endif %}"
        "{% if resource_quality %}✨ 宝藏光泽度：{{ resource_quality }}\n{% endif %}"
        "📦 箱子数量：{{ file_count }}个\n"
        "⚓ 总重量：{{ total_size }}\n"
        "🗺️ 宝藏代码 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}🦜 发现船队：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 寻宝用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 8：🧪「疯狂科学家」主题 🧪

```json
{
    "title": "💥 {{ title_year }} {{ season_episode if season_episode else '' }} - 实验成功！新成果诞生！🤯",
    "text": (
        "{% if vote_average %}🌟 科学价值：{{ vote_average }}/10 💡\n{% endif %}"
        "🔬 实验类型：{{ media_type }}\n"
        "{% if category %}🏷️ 研究领域：{{ category }}\n{% endif %}"
        "{% if resource_quality %}📊 数据精度：{{ resource_quality }}\n{% endif %}"
        "⚗️ 样本数量：{{ file_count }}个\n"
        "🧮 数据大小：{{ total_size }}\n"
        "🧬 项目编号 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}🧑‍🔬 研发团队：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 实验用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 9：🎨「艺术画廊」主题 🎨

```json
{
    "title": "🖼️ 「{{ title_year }} {{ season_episode if season_episode else '' }}」已加入您的私人画廊！✨",
    "text": (
        "{% if vote_average %}🌟 艺术鉴赏评分：{{ vote_average }}/10\n{% endif %}"
        "📚 作品流派：{{ media_type }}\n"
        "{% if category %}🏷️ 艺术风格：{{ category }}\n{% endif %}"
        "{% if resource_quality %}🖌️ 画质/细节：{{ resource_quality }}\n{% endif %}"
        "🖼️ 展品数量：{{ file_count }}件\n"
        "💾 存储需求：{{ total_size }}\n"
        "🎨 画廊编号 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}👨‍🎨 创作者/来源：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 策展用时：{{ time_usage }}\n{% endif %}"
    )
}
```

## 模板 10：📚「魔法图书馆」主题 📚

```json
{
    "title": "📜 「{{ title_year }} {{ season_episode if season_episode else '' }}」新魔法书已入馆！🔮",
    "text": (
        "{% if vote_average %}🌟 魔法强度/稀有度：{{ vote_average }}/10 ✨\n{% endif %}"
        "📚 魔法类型：{{ media_type }}\n"
        "{% if category %}🏷️ 咒语分类：{{ category }}\n{% endif %}"
        "{% if resource_quality %}💎 魔法纯度/清晰度：{{ resource_quality }}\n{% endif %}"
        "📜 卷轴/章节：{{ file_count }}个\n"
        "📖 书籍厚度/大小：{{ total_size }}\n"
        "🪄 图书馆索引 (TMDB)：{{ tmdbid }}\n"
        "{% if release_group %}🧙‍♂️ 抄写员/发现者：{{ release_group }}\n{% endif %}"
        "{% if time_usage %}⏱️ 抄录/入库用时：{{ time_usage }}\n{% endif %}"
    )
}
```
